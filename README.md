# api-token-replay-demo
A simple frontend demo that shows how token-based API authentication works and how tokens can be replayed to access protected endpoints.  Built for learning API security concepts, not exploitation.
